package com.springboot.jpa.spring_jdbc_jpa_hibernate.jpa;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

import com.springboot.jpa.spring_jdbc_jpa_hibernate.course.Course;

@Component
public class CourseJpaCommnadLineRunner //implements CommandLineRunner
{

	/*
	 * @Autowired private CourseJpaRepository repository; //private
	 * CourseJdbcRepository repository;
	 * 
	 * @Override public void run(String... args) throws Exception {
	 * 
	 * repository.insert(new Course(1,"J2EE","Harshul")); repository.insert(new
	 * Course(2,"Spring Jdbc","Nayan")); repository.insert(new
	 * Course(3,"Spring boot","Amit")); repository.insert(new
	 * Course(4,"Microservices","Tiru"));
	 * 
	 * repository.deleteById(4);
	 * 
	 * System.out.println(repository.findbyId(3));
	 * System.out.println(repository.findbyId(2));
	 * 
	 * }
	 */
	
	

}
